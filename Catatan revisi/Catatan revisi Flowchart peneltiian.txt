==================================
Revisi November Q1
===================================

1. Judul fiks jadi karena di revisi dosen pembimbing : Reaksi Publik terhadap Kinerja Menteri Keuangan RI: Analisis Sentimen menggunakan Algoritma BERT

2. Pelajari:
 - apa itu korpus = sekumpulan dokumen yang berisi data berupa teks
 - Tahapan umum NLP = pengumpulan data (korpus), preprocessing, TF-IDF, klasifikasi, evaluasi, interpretasi
 - Tahapan analisis sentiment = pengumpulan korpus, preprocessing, pelabelan manual (10%), ekstraksi fitur (TF-IDF ke korpus yang sudah dilabeli), pembuatan model klasifikasi, prediksi otomatis ke seluruh korpus, evaluasi.
 - Tahapan bert? = pengumpulan data, preprocessing ringan (tanpa stemming dan stopword removal), pelabelan manual, tokenisasi + tambahkan dense layer, fine tuning model, klasifikasi otomatis, evaluasi, topic modelling, interpretasi tiap kelas
 - Keunggulan bert = Bisa memahami makna kalimat: dengan menyimpang kata dalam bentuk embbedings vector kemudian dilanjutkan dengan transformer yang punya banyak layer (memahami hubungan antar kata dengan mekanisme self-attention (membandingkan apakah kata lain mempengaruhi aku dengan 3 parameter yaitu querry (apa yang dicari kata ini), key (apa yang dimiliki kata lain), dan value (informasi makna kata lain)))
 - bert itu termasuk n-gram yang mana? apa malah bukan? = bukan, karena N-gram hanya bisa melihat tetangga dan harus urut. sedangkan BERT bisa melihat semua kalimat dan ga harus urut karena ada mekanisme self-attention
 - pelajari tentang supervised dan unsupervised terkait klasifikasi topik? karna harus ada aturan seperti apa yang positif, negatif atau netral. = udah paham, intinya kalo supervised learning itu ada panduan label manualnya. dan harus jelas yang di labelin positif itu kalimat yang seperti apa, dll.
- Tinjauan pustaka (apa itu, syarat, jumlah tinjauan pustaka, dll) = intinya penelitian terdahulu. sumber data, objek, metode, hasil akhir, harus ada yang sama dan relevan untuk mencari gap researchnya

3. Terkait flowchart:
 - Cara bikin flowchart harus sesuai IPO (bentu kotak, ddl (gatau IPO apaan))
 - penulisan langkah harus menggunakan kata kerja (yang perlu, misal data duplikat harusnya ditulis menghapus data duplikat)
 - harus di tokenisasi dll dulu sebelum preprocessing, klasifikasi, dll (kata bu fida, manut aja tp konfirm ke gemini juga).
 - gausah cantumin hal yang terlalu teknis (misal TF-IDF ya tulis TF-IDF aja gausah TF, IDF, TF-IDF, dll)
 - gausah ada fase rekomendasi, cukup sampe interpretasi aja (gapunya kapasitas kata bu fida)

4. Cari tau untuk menemukan topik tertentu dan menggolongkannya kedalam beberapa tema (mungkin BERT ada tools nya) = pakai BERTopic, jalankan per kelas

4. cari tinjauan Pustaka

=====================================================

=================================
Revisi November Q2
=================================
Terkait Flowchart (DONE)
1. Hanya ada 4 fase: dataset, preprocessing, klasifikasi, analisis
2. pada preprocessing harus dijelasin apa aja prosesnya (dibuat fase:  hapus duplikat, tokenisasi, cleaning, lowercase, normalisasi?)
3. gausah ada fine tuning. pokoknya ikutin alur nomer 1 aja udah
4. evaluasi dulu baru output dataset terklasifikasi
5. MAKEE IT SIMPLE BROTHERRRR

Pelajari (done?)
1. gimana kalo missal gapake fine tuning, apakah ada pelabelan manual? = tidak ada
2. apakah ada evaluasi kalo ga pake fine tuning? jika iya. pelajari apa itu data training, validasi, testing. dan dari mana sumber datanya untuk masing masing itu (apakah dari dataset atau beda dataset?) = tetap ada, dilabelin manual dengan aturan yang sudah disiapkan. kemudian label manual dibandingkan dengan label otomatis

Terkait tinjauan Pustaka:
1. cari 10 tinjauan Pustaka pada jurnal yg bagus (5 yang pake BERT, 5 yang objeknya mentri)
2. yang harus dirangkum: judul, nama, tahun, objek penelitian, sumber & jumlah data, hasil Utama/temuan
3. semua penjelasan rangkuman kalo bisa pake angka datanya (jangan mayoritas, dll)
4. RANGKUM MANUALLL JANGAN PAKE AI BOSSSS
======================================

======================================================
Catatan Revisi November Q3 - sudah dibolehkan buat proposal
======================================================
Tentang flowchart:
1. dari lowercasing lanjut ke splitting data
2. Klasifikasi menggunakan BERT, jangan cuma klasifikasi
3. dataset terklasifikasi dulu baru evaluasi
4. evaluasi menggunakan matrisk evaluasi apa aja (ada 4)
5. gausah ada proses interpretasi, langsung ke interpretasi

Tambahan:
1. tamnbahkan contoh kalimat pada aturan klasifikasi
2. jangan pake Bahasa yang susah, pake yang paling sederhana aja
3. coba cari kamus BERT yang bisa memahami sarkasme
4. Cari proporsi pembagian data evaluasi (training, validation, testing) lewat artikel
5. dataset evaluasi diambil dari dataset yg berbeda